/**
 * For a given parameter it checks whether param is null or undefined.
 * @param {unknown} param 
 */
export function isNullOrUndefined(param) {
    return param === null || param === undefined;
}